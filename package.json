{
  "name": "faceblindness-aid-app",
  "version": "1.0.0",
  "description": "AI-powered assistive app for people with face blindness",
  "main": "index.js",
  "scripts": {
    "start": "cd client && npm start",
    "android": "cd client && npm run android",
    "ios": "cd client && npm run ios",
    "web": "cd client && npm run web",
    "backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install-all": "npm install && cd client && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "keywords": [
    "react-native",
    "expo",
    "fastapi",
    "supabase",
    "face-recognition"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "expo": "^53.0.19"
  }
}
